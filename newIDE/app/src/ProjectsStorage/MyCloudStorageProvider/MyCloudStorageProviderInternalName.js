// @flow

// Kept in its own file so it can be imported by the resource mover/fetcher
// dispatch maps without pulling in the full provider (which imports React/UI).
export const myCloudStorageProviderInternalName = 'MyCloud';
